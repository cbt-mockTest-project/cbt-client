import ExamReviewComponent from '@components/examReview/ExamReviewComponent';
import { NextPage } from 'next';

const ExamsReviewPage: NextPage = () => {
  return <ExamReviewComponent />;
};

export default ExamsReviewPage;
