import React from 'react';
import styled from 'styled-components';

const RegisterTabBlock = styled.div``;

interface RegisterTabProps {}

const RegisterTab: React.FC<RegisterTabProps> = () => {
  return (
    <RegisterTabBlock>
      {/* 컴포넌트의 내용을 여기에 작성하세요. */}
    </RegisterTabBlock>
  );
};

export default RegisterTab;
