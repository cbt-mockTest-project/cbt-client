import React from 'react';
import styled from 'styled-components';

const BookmarkOptionBoxBlock = styled.div``;

interface BookmarkOptionBoxProps {}

const BookmarkOptionBox: React.FC<BookmarkOptionBoxProps> = () => {
  return (
    <BookmarkOptionBoxBlock>
      {/* 컴포넌트의 내용을 여기에 작성하세요. */}
    </BookmarkOptionBoxBlock>
  );
};

export default BookmarkOptionBox;
